export enum CATEGORY_STATUS {
  DRAFT = 'draft',
  ACTIVE = 'active',
  DELETED = 'deleted',
}

export enum USER_ACTIVE_STATUS {
  ACTIVE = 1,
  INACTIVE = 0,
}

export enum USER_STAFF_STATUS {
  ACTIVE = 1,
  INACTIVE = 0,
}

export enum USER_ADMIN_STATUS {
  ACTIVE = 1,
  INACTIVE = 0,
}

export enum MEMBER_SHIP_STATUS {
  ACTIVE = 'active',
}

export enum MEMBER_SHIP_ACTIVE_STATUS {
  ACTIVE = 1,
  INACTIVE = 0,
}

export enum EMAIL_ADDRESS_VERIFY_STATUS {
  VERIFIED = 1,
  NOT_VERIFIED = 0,
}

export enum PRODUCT_STATUS {
  PUBLISHED = 'published',
  DELETED = 'deleted',
}
